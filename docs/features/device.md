# 设备信息

设备信息用于社区签到、米游社请求和部分游戏接口。每个 profile 独立保存设备数据。

常用指令：

- `#绑定设备2`
- `#绑定设备信息2`
- `#原神绑定设备2`
- `#星铁绑定设备2`
- `#绝区零绑定设备2`
- `#设备信息2`

绑定内容可以是完整设备 JSON，也可以是已有的 `device_id/device_fp`。资料卡和设备信息页会脱敏展示。

设备信息不依赖外部 device-plugin；荷花会在自己的米游社请求封装里注入设备字段。
